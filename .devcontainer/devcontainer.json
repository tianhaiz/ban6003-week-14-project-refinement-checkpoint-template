{
  "image": "mcr.microsoft.com/devcontainers/miniconda:3",
  "features": {},
  "postCreateCommand": "conda install -y -n base python=3.12 pip && /opt/conda/bin/python -m pip install --upgrade pip && /opt/conda/bin/python -m pip install -r requirements.txt && /opt/conda/bin/python -m ipykernel install --user --name base --display-name \"base\"",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-toolsai.jupyter"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/opt/conda/bin/python",
        "python.terminal.activateEnvironment": true,
        "jupyter.askForKernelRestart": false
      }
    }
  },
  "forwardPorts": [],
  "remoteUser": "vscode"
}
